kind: core/block-type
metadata:
  name: blockware/block-type-frontend
  title: Frontend

spec:
  schema:
    type: object
    properties:
      target:
        $ref: core/language-target
      entities:
        $ref: core/entity-list
      consumers:
        $ref: core/resource-list
      providers:
        $ref: core/resource-list
    required:
      - target

